@isTest
global class MockIoTAPI implements HttpCalloutMock {

    global HTTPResponse respond(HTTPRequest req) {

        HttpResponse res = new HttpResponse();
        res.setStatusCode(200);
        res.setBody('{"registrationId":"DEV123","status":"OK"}');
        return res;
    }
}